<p id="notice"><%= notice %></p>

<h1>Журнал ежедневный</h1>


<%= link_to 'Добавить новую дату', new_date_job_path, class: "btn btn-sm btn-default" %>
<p></p>

<table class="table table-bordered">
  <thead>
    <tr class="active">
      
      <th colspan="3">Дата</th>
      
    </tr>
  </thead>

  <tbody>
    <% @date_jobs.each do |date_job| %>
      <tr>
        
        <td><%= link_to date_job.date.strftime("%d/%m/%Y"), date_job %></td>
        <td style="text-align: right"><%= link_to 'Изменить', edit_date_job_path(date_job) %></td>
        <td><%= link_to 'Удалить', date_job, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Добавить новую дату', new_date_job_path, class: "btn btn-sm btn-default" %>
